You are fixing Japanese ruby readings for one rejected local span.

Input:
- Text: {text}
- Current yomi: {current_yomi}
- Rejected span: {rejected_span}
- Rejected readings: {rejected_readings}

Task:
Return the correct reading for the rejected span.
Determine the reading in context, then return natural word-level segments. Being an established name/entity/term does not make the whole span a single word.
The current segmentation may be wrong; consider that one original word may need two or more items, or multiple original items may need one item.
The rejection may concern segmentation only. Some or all existing readings may already be correct. Preserve correct readings and change only the boundaries or readings that need correction.
The span may also have been escalated because the human reviewer could not confidently determine its reading. Treat the existing reading as uncertain, not necessarily wrong, and resolve it from context or evidence.
For personal names, return separate items for the family name and given name when identifiable, even if the full name is well known or written without a space. Keep each family name and each given name intact; do not split them into individual kanji.
For school, organization, publication, and place names, separate the distinctive name from a common institutional or descriptive suffix when natural. Also separate productive prefixes or modifiers from the word they modify when they form a natural boundary.
Do not maximize splitting: preserve ordinary lexical words and established compounds internally. Use one item when there is no natural word boundary, not merely because the whole span refers to one entity. Do not invent a boundary when the name structure is uncertain.
Whitespace is always a hard token boundary. Never include whitespace in an item's surface; return separate items for the components on each side and omit the whitespace itself from the array.
If the rejected span is a symbolic kaomoji, keep it as one item and use カオモジ; do not read or split its individual symbols. A matching pair of parentheses containing only Japanese text, such as （笑）, is semantic text rather than this fallback; Japanese characters used as parts of a symbolic face do not by themselves disqualify it.
If you know the reading of the span, answer directly without web search.
If you do not know it or are not confident, use web search, find the reading that fits this context, and output the final JSON together with your investigation process.
Include a concise comment in Japanese explaining the decision or evidence in each item.
Format: [{{"surface":"（表記）", "reading":"（読み）", "used_web_search":true/false, "comment":"（簡潔な判断理由または根拠）"}}...]
