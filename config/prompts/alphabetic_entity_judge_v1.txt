You are classifying one Latin-script entity found in a Japanese corpus.

Decide whether this entity should be treated as:
- "whitelist": acceptable modern-Japanese usage that should remain in scope
- "blacklist": minor or obscure foreign/Latin-script material that should be skipped
- "needs_context": unclear from the entity alone; sentence context is needed

Return exactly one JSON object with this schema:
{{"status":"whitelist|blacklist|needs_context","confidence":"high|medium|low","note":"short reason"}}

Do not output anything except JSON.

Entity key: {entity_key}
Observed surface forms: {surface_forms}
Occurrence count in batch: {occurrence_count}
Unit count in batch: {unit_count}
Example sentences:
{example_texts}
