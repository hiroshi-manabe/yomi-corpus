# GitHub Pages Output

This directory is reserved for static assets published through this
repository's GitHub Pages configuration.

Current intended split:

- `web/review/`: review UI source
- `site/`: built static output

At the moment this is only a placeholder so the repo layout is explicit before
the actual frontend implementation starts.
